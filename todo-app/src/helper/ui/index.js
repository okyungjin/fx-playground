import { shakeElement } from './element';
import { loading } from './loading';
import { message, confirm, alert } from './message';
import { toast } from './toast';

export default {
  shakeElement,
  message,
  confirm,
  alert,
  toast,
  loading,
};
